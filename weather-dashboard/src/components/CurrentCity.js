import React from 'react';

const CurrentCity = (props) => (
  <div>
    <h1> Weather app </h1>
    <img alt="weather icon" src="http://openweathermap.org/img/w/10d.png" />
    <p>Name:</p>
    <p>Description:</p>
    <p>Current Temp:</p>
    <p>Temp Min:</p>
    <p>Temp Max:</p>
    <p>Wind Speed:</p>
  </div>
);

export default CurrentCity;
